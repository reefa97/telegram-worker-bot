# Telegram Worker Tracking System

Полноценная система управления работниками через Telegram бот с веб-панелью администратора. Система позволяет отслеживать рабочие смены работников, их местоположение, назначать объекты и управлять доступами.

## 🎯 Основные возможности

- **Telegram Bot**: Работники отмечают начало/окончание работы, отправляют GPS локацию
- **Веб-панель администратора**: Управление работниками, объектами, просмотр отчетов
- **Многоуровневый доступ**: Super Admin и Sub Admin с изоляцией данных
- **Отчеты**: Статистика по рабочим сменам с фильтрацией
- **Уведомления**: Автоматические уведомления в Telegram при начале/окончании работы

## 📋 Технологический стек

- **Frontend**: React 18 + TypeScript + Vite + Tailwind CSS
- **Backend**: Supabase (PostgreSQL + Edge Functions)
- **Bot**: Telegram Bot API + Deno Runtime
- **Auth**: Supabase Authentication

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка Supabase

#### Создайте проект Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Скопируйте URL и Anon Key

#### Примените миграции базы данных

В консоли Supabase (SQL Editor) выполните файлы миграций по порядку:

1. `supabase/migrations/20240101000000_initial_schema.sql`
2. `supabase/migrations/20240101000001_security_functions.sql`
3. `supabase/migrations/20240101000002_rls_policies.sql`
4. `supabase/migrations/20240101000003_indexes.sql`

#### Разверните Edge Functions

Установите Supabase CLI:

```bash
npm install -g supabase
```

Войдите в Supabase:

```bash
supabase login
```

Свяжите проект:

```bash
supabase link --project-ref your-project-ref
```

Разверните все функции:

```bash
supabase functions deploy telegram-bot
supabase functions deploy send-message
supabase functions deploy create-admin
supabase functions deploy delete-sub-admin
supabase functions deploy delete-super-admin
```

### 3. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните переменные:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

### 4. Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте токен бота (например: `123456789:ABCdefGHIjklMNOpqrSTUvwxYZ`)

### 5. Настройка Webhook

Настройте webhook для вашего бота:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://your-project.supabase.co/functions/v1/telegram-bot"}'
```

Проверьте статус webhook:

```bash
curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
```

### 6. Запуск приложения

Запустите в режиме разработки:

```bash
npm run dev
```

Откройте [http://localhost:5173](http://localhost:5173)

### 7. Первый вход

1. Откройте веб-панель
2. **Первый пользователь** автоматически станет Super Admin
3. Войдите с любым email/password (Supabase Auth создаст аккаунт)
4. Перейдите в **Настройки** → вставьте токен Telegram бота → активируйте бота

## 📖 Использование системы

### Добавление работника

1. Откройте вкладку **Работники**
2. Нажмите **Добавить работника**
3. Заполните данные: имя, фамилию, телефон, зарплату
4. Выберите объекты работы (можно выбрать несколько)
5. Нажмите **Создать**
6. Скопируйте ссылку активации (иконка копирования)
7. Отправьте ссылку работнику в Telegram

### Активация работника

Работник должен:

1. Перейти по ссылке активации
2. Нажать **Start** в боте
3. Бот активирует аккаунт и покажет кнопки управления

### Рабочая смена

**Начало работы:**

1. Работник нажимает **▶️ Начать работу**
2. Выбирает объект (если их несколько)
3. Отправляет местоположение (GPS)
4. Бот создает сессию и уведомляет администраторов

**Окончание работы:**

1. Работник нажимает **🛑 Закончить работу**
2. Отправляет местоположение
3. Бот фиксирует окончание и считает длительность
4. Администраторы получают уведомление с картой и временем

### Настройка уведомлений

1. Откройте **Настройки** в веб-панели
2. В разделе **Администраторы для уведомлений** добавьте Chat ID
3. Чтобы узнать свой Chat ID:
   - Напишите [@userinfobot](https://t.me/userinfobot)
   - Он отправит вам ваш Chat ID
   - Скопируйте и вставьте в веб-панель

### Создание Sub Admin

1. Откройте вкладку **Sub Admins**
2. Нажмите **Добавить Sub Admin**
3. Введите email и пароль
4. Sub Admin сможет:
   - Создавать своих работников
   - Создавать своих Sub Admins
   - Видеть только свои данные

## 🗄️ Структура базы данных

### Основные таблицы

- **admin_users** - Администраторы (Super Admin, Sub Admin)
- **workers** - Работники с данными о зарплате и Telegram
- **cleaning_objects** - Объекты работы (офисы, склады и т.д.)
- **worker_objects** - Связь работников с объектами (многие-ко-многим)
- **work_sessions** - Рабочие смены с GPS координатами
- **bot_settings** - Настройки Telegram бота
- **bot_admins** - Chat IDs для уведомлений

### Безопасность (RLS)

Все таблицы защищены Row Level Security:

- **Super Admin** видит все данные
- **Sub Admin** видит только свои данные и данные созданных им пользователей
- **Workers** не имеют прямого доступа к базе (только через бота)

## 🔧 Edge Functions

### telegram-bot

Основной webhook бота. Обрабатывает:

- Команды `/start` с активацией по токену
- Кнопки "Начать работу" и "Закончить работу"
- GPS координаты
- Выбор объектов работы
- Отправку уведомлений администраторам

### send-message

Отправка сообщений работникам из веб-панели.

### create-admin

Создание Super Admin или Sub Admin через веб-панель.

### delete-sub-admin / delete-super-admin

Удаление администраторов с проверкой прав доступа.

## 📊 Отчеты

Вкладка **Отчеты** показывает:

- Список всех рабочих смен
- Фильтрацию по датам
- Статистику: общее время, среднюю длительность
- Детали каждой смены: работник, объект, время начала/окончания

## 🛠️ Разработка

### Запуск в режиме разработки

```bash
npm run dev
```

### Сборка для продакшена

```bash
npm run build
```

### Проверка типов TypeScript

```bash
npm run type-check
```

## 🌐 Деплой

### Frontend

Можно задеплоить на любой платформе для статических сайтов:

- **Vercel**: `vercel deploy`
- **Netlify**: `netlify deploy`
- **GitHub Pages**: `npm run build` → загрузите `dist/`

### Backend

Backend уже развернут на Supabase (Edge Functions + Database).

## 📝 Важные заметки

1. **Первый пользователь** автоматически становится Super Admin
2. **Bot Username**: Замените `YOUR_BOT_USERNAME` в `WorkersPanel.tsx` на username вашего бота
3. **RLS политики** обеспечивают изоляцию данных между админами
4. **Webhook** должен быть настроен для работы бота
5. **Chat ID** администраторов нужен для уведомлений

## 🐛 Устранение неполадок

### Бот не отвечает

- Проверьте, что webhook настроен правильно
- Проверьте, что токен бота введен в настройках
- Проверьте, что бот активирован в настройках
- Проверьте логи Edge Function в Supabase

### Работник не может активироваться

- Убедитесь, что ссылка скопирована полностью
- Проверьте, что токен не истек
- Проверьте, что бот запущен (/start)

### Уведомления не приходят

- Проверьте, что Chat ID администраторов добавлены
- Проверьте, что администраторы активны
- Убедитесь, что бот отправил хотя бы одно сообщение админу

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении проблем проверьте:

1. Логи Supabase Edge Functions
2. Консоль браузера (DevTools)
3. Статус webhook бота

---

**Создано с использованием React, TypeScript, Supabase и Telegram Bot API**
